general:
steps:
  mtaBuild:
    buildTarget: 'CF'

  cloudFoundryDeploy:
    deployTool: 'mtaDeployPlugin'
    deployType: 'standard'
    cloudFoundry:
      org: 'a30ca3b9trial'
      space: 'dev'
      credentialsId: 'CF_CREDENTIALSID'